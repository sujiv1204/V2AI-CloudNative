apiVersion: v1
kind: Secret
metadata:
  name: gcp-credentials
  namespace: v2ai
type: Opaque
# Note: Run this command to create the secret with actual key:
# kubectl create secret generic gcp-credentials --from-file=gcp-key.json=./gcp-key.json -n v2ai
# This file is a placeholder template
